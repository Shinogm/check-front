{
  "name": "check",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd front-end && pnpm i && pnpm dev\" \"cd back-end && pip install -r requirements.txt && py main.py\""
  }
}
